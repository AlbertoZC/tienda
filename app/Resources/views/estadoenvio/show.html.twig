{% extends 'base.html.twig' %}

{% block body %}
    <h1>Estadoenvio</h1>

    <table>
        <tbody>
            <tr>
                <th>Estado</th>
                <td>{{ estadoenvio.estado }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('estadoenvio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('estadoenvio_edit', { 'estado': estadoenvio.estado }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
