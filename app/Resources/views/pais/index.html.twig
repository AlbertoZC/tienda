{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pais list</h1>

    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pai in pais %}
            <tr>
                <td><a href="{{ path('pais_show', { 'nombre': pai.nombre }) }}">{{ pai.nombre }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pais_show', { 'nombre': pai.nombre }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('pais_edit', { 'nombre': pai.nombre }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pais_new') }}">Create a new pai</a>
        </li>
    </ul>
{% endblock %}
